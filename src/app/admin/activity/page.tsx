import { PlaceholderPage } from "@/components/placeholder-page";
import { History } from "lucide-react";

export default function ActivityPage() {
  return (
    <PlaceholderPage
      title="Activity Log"
      description="Track all significant user and admin actions for auditing purposes."
      icon={History}
    />
  );
}
